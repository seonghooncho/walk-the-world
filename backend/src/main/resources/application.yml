spring:
  application:
    name: timelink-api

  profiles:
    default: local

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  servlet:
    context-path: /

oauth:
  allowed-frontend-origins: ${OAUTH_ALLOWED_FRONTEND_ORIGINS:}
  public-api-base-url: ${OAUTH_PUBLIC_API_BASE_URL:}
  google:
    allowed-client-ids: ${OAUTH_GOOGLE_ALLOWED_CLIENT_IDS:}
  kakao:
    client-id: ${KAKAO_CLIENT_ID:}
    client-secret: ${KAKAO_CLIENT_SECRET:}
